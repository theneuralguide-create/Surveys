/* ===== Survey Hub configuration =====
   Paste your Supabase project's URL and anon (public) key here.
   Find them in: Supabase dashboard → Project Settings → API keys.

   Until these are filled in, the site runs in "local mode": every page and
   survey still works, but responses stay on each visitor's own device and
   the admin dashboard can't show pooled results. */
window.SURVEY_HUB_CONFIG = {
  url: 'https://zaftmandamqwxrxsibyk.supabase.co',        /* e.g. 'https://abcdefghijkl.supabase.co' */
  anonKey: 'sb_publishable_mauZK5A_2i7sqxl5aB8xtQ_-XhIHJs7'
};
